# Contributing to NARAVA

All changes must preserve the source-of-truth hierarchy and maintain traceability from implementation to capability, rule, invariant and business purpose.

Before submitting changes:
- run relevant tests;
- update ADRs for architectural decisions;
- update documentation when contracts change;
- do not commit secrets;
- do not silently alter locked semantics.
