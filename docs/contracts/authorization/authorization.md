# Authorization Contract

Evaluate identity, membership, role, permission, organization/tenant scope, resource ownership, current state and capability.

A role enum alone is not sufficient authorization.
